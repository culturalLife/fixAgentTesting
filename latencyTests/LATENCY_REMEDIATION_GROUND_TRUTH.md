# Latency Scenarios & Auto-Fix Ground Truth Reference

This document serves as the **ground-truth benchmark and evaluation rubric** for the Observability Auto-Fix system. It specifies the exact trace parameters, root-cause signatures, expected judge verdicts, and concrete code/configuration fixes for all 12 latency error scenarios implemented in [`latencyTests/`](./).

Use this reference to compare the remediation cards and code diffs generated by the Auto-Fix engine against the intended ground truth.

---

## Summary Matrix

| # | Scenario | Latency Defect Type | Root Cause Category | Expected Judge Finding | Ground-Truth Auto-Fix Action | Expected Latency Reduction |
|---|---|---|---|---|---|---|
| **1** | **Cascading Handoff Delay** | `CASCADING_HANDOFF_DELAY` | Upstream Bottleneck | `LATENCY_BOTTLENECK_DETECTED` | Stream chunks asynchronously & parallelize page OCR rasterization | ~70% (3.2s $\rightarrow$ 0.9s) |
| **2** | **Ping-Pong Loop Thrash** | `LOOP_THRASH_HANDOFF` | Agent Thrashing | `LOOP_THRASH_DETECTED` | Introduce strict state schema & max 2-turn handoff breakout guard | ~75% (7.8s $\rightarrow$ 1.8s) |
| **3** | **Blocking Tool / I/O Stall** | `BLOCKING_TOOL_STALL` | Synchronous Tool I/O | `SLOW_TOOL_EXECUTION` | Convert synchronous ERP query to async non-blocking with connection pool | ~80% (3.6s $\rightarrow$ 0.7s) |
| **4** | **Token Bloat & High TTFT** | `TOKEN_BLOAT_LATENCY` | Excessive Context Payload | `TOKEN_BLOAT_LATENCY` | Inject pre-handoff extractive summarization filter (< 2k tokens) | ~60% (2.8s $\rightarrow$ 1.1s) |
| **5** | **Fan-out Straggler Bottleneck** | `FANOUT_STRAGGLER_DELAY` | Concurrency Imbalance | `ASYNC_FANOUT_STRAGGLER` | Add 1.5s per-agent SLA timeout with cached fallback for stragglers | ~65% (3.9s $\rightarrow$ 1.4s) |
| **6** | **Retry Storm & Degraded Fallback** | `RETRY_STORM_BACKOFF` | Unmitigated Retry Storm | `RETRY_STORM_LATENCY` | Circuit breaker + single fast-fail retry before queuing offline batch | ~85% (5.4s $\rightarrow$ 0.8s) |
| **7** | **Model Oversizing LLM Delay** | `MODEL_OVERSIZING_LLM_DELAY` | Model Compute Overkill | `MODEL_OVERSIZING_DETECTED` | Downgrade model from `mistral-large-latest` to `mistral-small-latest` | ~75% (4.5s $\rightarrow$ 0.9s) |
| **8** | **Unconstrained Generation Drag** | `UNCONSTRAINED_GEN_DRAG` | Runaway Output Tokens | `RUNAWAY_OUTPUT_GENERATION` | Add `max_tokens: 150` & enforce strict structured JSON schema | ~70% (4.5s $\rightarrow$ 1.2s) |
| **9** | **Sequential I/O Waterfall** | `SEQUENTIAL_IO_WATERFALL` | Sequential Network I/O | `SEQUENTIAL_IO_BOTTLENECK` | Replace sequential loop with `asyncio.gather(*fetch_tasks)` | ~75% (3.4s $\rightarrow$ 0.85s) |
| **10** | **Uncached Repeated I/O Lookup** | `UNCACHED_REPEATED_IO` | Idempotent I/O Redundancy | `UNCACHED_IDEMPOTENT_IO` | Wrap tool with Redis / `@lru_cache(ttl=3600)` decorator | ~50% (3.2s $\rightarrow$ 1.6s) |
| **11** | **Heavy Vision Payload I/O** | `HEAVY_VISION_PAYLOAD_IO` | Uncompressed Multi-modal I/O | `UNCOMPRESSED_VISION_PAYLOAD` | Downsample image to 1024px WebP / extract text before LLM call | ~85% (4.2s $\rightarrow$ 0.5s) |
| **12** | **Clean Optimal Baseline** | `CLEAN_OPTIMAL_BASELINE` | Optimal (Control Case) | `OPTIMAL_PERFORMANCE` | None (No anomaly, control reference) | 0% (Already optimal) |

---

## Detailed Scenario Ground Truths

```
====================================================================================================
```

### Scenario 1: Cascading Handoff Delay
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L93-L177)
- **Function**: `run_cascading_delay_scenario`
- **Latency Type**: `CASCADING_HANDOFF_DELAY`

#### 1. Diagnostic Parameters
- **Span Name**: `agent_documentingestionagent_parse_and_chunk_pdf`
- **Duration**: ~3.2s (dominating 85%+ of overall 5.5s trace)
- **Attributes**: `gen_ai.latency.type: CASCADING_HANDOFF_DELAY`, `document_pages: 48`
- **Child Tool**: `ocr_pdf_rasterize` (takes 3.2s)

#### 2. Expected Auto-Fix
- **Action**: Parallelize page rasterization using a process pool or stream parsed chunks to downstream agents immediately as they are processed rather than waiting for all 48 pages to complete.

```diff
# BEFORE (Blocking all-at-once ingestion)
- all_chunks = await ocr_pdf_rasterize(document_pages=48)
- await entity_extraction_agent(all_chunks)

# AFTER (Streaming / Parallelized chunk handoff)
+ async for chunk_batch in ocr_stream_pages_parallel(document_pages=48, batch_size=5):
+     asyncio.create_task(entity_extraction_agent(chunk_batch))
```

```
====================================================================================================
```

### Scenario 2: Ping-Pong Loop Thrash Handoff
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L180-L255)
- **Function**: `run_ping_pong_loop_scenario`
- **Latency Type**: `LOOP_THRASH_HANDOFF`

#### 1. Diagnostic Parameters
- **Span Count**: 8+ alternating spans (`agent_triageagent_evaluate_claim_turn_*` $\leftrightarrow$ `agent_policyvalidationagent_verify_clauses_turn_*`)
- **Duration**: 7.8s - 17.6s cumulative
- **Attributes**: `gen_ai.agent.handoff.iteration: 1..4`, `rejection_code: AMBIGUOUS_EVIDENCE`

#### 2. Expected Auto-Fix
- **Action**: Enforce an explicit handoff contract schema with a hard loop limit (`max_iterations=2`), escalating unresolved ambiguity directly to a Human-in-the-Loop or an Arbitrator agent.

```diff
# BEFORE (Unbounded back-and-forth handoff loop)
- while not policy_resolved:
-     clarification = await triage_agent.ask(claim)
-     policy_resolved = await policy_agent.evaluate(clarification)

# AFTER (Bounded loop with early arbitration breakout)
+ for iteration in range(1, MAX_TURNS + 1):
+     clarification = await triage_agent.ask(claim, structured_schema=True)
+     result = await policy_agent.evaluate(clarification)
+     if result.is_resolved or iteration >= 2:
+         return await arbitrator_agent.resolve_dispute(claim, result)
```

```
====================================================================================================
```

### Scenario 3: Blocking Tool / External I/O Stall
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L258-L335)
- **Function**: `run_blocking_tool_stall_scenario`
- **Latency Type**: `BLOCKING_TOOL_STALL`

#### 1. Diagnostic Parameters
- **Span Name**: `tool_query_legacy_sap_erp`
- **Duration**: ~3.6s (70%+ of overall 5.2s trace)
- **Attributes**: `gen_ai.tool.name: query_legacy_sap_erp`, `timeout_sec: 10`

#### 2. Expected Auto-Fix
- **Action**: Convert synchronous legacy ERP database queries into an asynchronous non-blocking connection with a fast read replica and 1.5s timeout.

```diff
# BEFORE (Blocking synchronous socket I/O)
- erp_data = synchronous_legacy_sap_query(order_id)

# AFTER (Async non-blocking connection with tight timeout)
+ erp_data = await async_sap_client.query_order_async(order_id, timeout=1.5)
```

```
====================================================================================================
```

### Scenario 4: Token Bloat & High TTFT Latency
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L338-L415)
- **Function**: `run_token_bloat_scenario`
- **Latency Type**: `TOKEN_BLOAT_LATENCY`

#### 1. Diagnostic Parameters
- **Span Name**: `agent_executivesummaryagent_synthesize_bloated_corpus`
- **Duration**: ~2.8s - 5.1s
- **Attributes**: `llm.prompt_tokens: 35400`, `llm.time_to_first_token_ms: 2400`

#### 2. Expected Auto-Fix
- **Action**: Prune context before handoff using an extractive text trimmer or map-reduce chunk summarization to keep prompt payload under 2,000 tokens.

```diff
# BEFORE (Passing 35k token unpruned dump to summary agent)
- summary = await summary_agent.run(raw_corpus_dump_35k_tokens)

# AFTER (Pre-filtering and extractive compression)
+ pruned_context = extract_key_passages(raw_corpus_dump, max_tokens=1500)
+ summary = await summary_agent.run(pruned_context)
```

```
====================================================================================================
```

### Scenario 5: Fan-out Straggler Bottleneck
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L418-L510)
- **Function**: `run_fanout_straggler_scenario`
- **Latency Type**: `FANOUT_STRAGGLER_DELAY`

#### 1. Diagnostic Parameters
- **Parallel Spans**:
  - `agent_identityverificationagent_verify_kyc_identity` (0.2s)
  - `agent_frauddetectionagent_score_fraud_risk` (0.3s)
  - `agent_creditbureaustraggleragent_pull_tri_bureau_credit_history` (3.9s)
- **Total Duration**: ~4.6s - 6.2s (gated entirely by Credit Bureau span)

#### 2. Expected Auto-Fix
- **Action**: Add an SLA timeout (`timeout=1.5s`) for straggler credit bureau calls with a fast fallback to cached credit tiering or hedging requests.

```diff
# BEFORE (Gating all agents on the slowest straggler)
- kyc, fraud, credit = await asyncio.gather(sub_identity(), sub_fraud(), sub_credit())

# AFTER (Bounded timeout with fallback on slow straggler)
+ async def sub_credit_with_sla():
+     try:
+         return await asyncio.wait_for(sub_credit(), timeout=1.5)
+     except asyncio.TimeoutError:
+         return await get_cached_credit_tier(applicant_id)
+
+ kyc, fraud, credit = await asyncio.gather(sub_identity(), sub_fraud(), sub_credit_with_sla())
```

```
====================================================================================================
```

### Scenario 6: Retry Storm & Degraded Fallback
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L513-L615)
- **Function**: `run_retry_storm_scenario`
- **Latency Type**: `RETRY_STORM_BACKOFF`

#### 1. Diagnostic Parameters
- **Spans**: 3 sequential failed `ach_payment_gateway` spans (`attempt: 1..3`) followed by `agent_degradedsettlementagent_queue_asynchronous_batch_payout`
- **Duration**: ~5.4s - 6.5s
- **Status**: `DEGRADED`

#### 2. Expected Auto-Fix
- **Action**: Implement a circuit breaker and reduce retries to 1 immediate attempt before offloading to the asynchronous settlement queue.

```diff
# BEFORE (3 sequential timeout attempts with escalating backoff)
- for attempt in range(1, 4):
-     res = await ach_gateway_call(attempt)
-     await asyncio.sleep(backoff[attempt])

# AFTER (Circuit breaker with fast-fail to async queue)
+ if circuit_breaker.is_open("ach_gateway"):
+     return await queue_asynchronous_batch_payout(payment)
+ try:
+     return await ach_gateway_call(timeout=1.0)
+ except GatewayTimeoutError:
+     circuit_breaker.record_failure("ach_gateway")
+     return await queue_asynchronous_batch_payout(payment)
```

```
====================================================================================================
```

### Scenario 7: Model Oversizing LLM Delay
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L710-L775)
- **Function**: `run_model_oversizing_scenario`
- **Latency Type**: `MODEL_OVERSIZING_LLM_DELAY`

#### 1. Diagnostic Parameters
- **Span Name**: `agent_heavytierclassifieragent_classify_vip_tier`
- **Model**: `mistral-large-latest`
- **Tokens**: `prompt_tokens: 85`, `completion_tokens: 8`
- **LLM Duration**: ~3.8s - 4.5s for a 2-word answer (`"VIP: YES"`)

#### 2. Expected Auto-Fix
- **Action**: Downgrade the LLM model from `mistral-large-latest` to `mistral-small-latest` or `ministral-8b-latest` for simple classification tasks.

```diff
# BEFORE (Oversized frontier model for trivial 2-word classification)
- response = await client.chat.complete_async(
-     model="mistral-large-latest",
-     messages=[{"role": "user", "content": "Answer ONLY 'VIP: YES' or 'VIP: NO'..."}]
- )

# AFTER (Fast, lightweight model suited for classification)
+ response = await client.chat.complete_async(
+     model="mistral-small-latest",  # or "ministral-8b-latest"
+     messages=[{"role": "user", "content": "Answer ONLY 'VIP: YES' or 'VIP: NO'..."}]
+ )
```

```
====================================================================================================
```

### Scenario 8: Unconstrained Generation Drag
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L778-L845)
- **Function**: `run_unconstrained_generation_scenario`
- **Latency Type**: `UNCONSTRAINED_GEN_DRAG`

#### 1. Diagnostic Parameters
- **Span Name**: `agent_verbosepolicyagent_draft_policy_explanation`
- **Tokens**: `completion_tokens: 1420` (Excessive output for simple inquiry)
- **LLM Duration**: ~4.5s - 5.2s

#### 2. Expected Auto-Fix
- **Action**: Add `max_tokens: 150` constraint and instruct the model to output strict 2-bullet format or a structured JSON response.

```diff
# BEFORE (Unbounded, open-ended prompt causing 1,400+ token generation)
- response = await client.chat.complete_async(
-     model="mistral-small-latest",
-     messages=[{"role": "user", "content": "Explain the return policy in detail..."}],
- )

# AFTER (Strict max_tokens and bullet-point constraint)
+ response = await client.chat.complete_async(
+     model="mistral-small-latest",
+     messages=[{"role": "user", "content": "Provide a 2-bullet summary of the 30-day return policy."}],
+     max_tokens=150,
+     temperature=0.1,
+ )
```

```
====================================================================================================
```

### Scenario 9: Sequential I/O Waterfall
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L848-L925)
- **Function**: `run_sequential_io_waterfall_scenario`
- **Latency Type**: `SEQUENTIAL_IO_WATERFALL`

#### 1. Diagnostic Parameters
- **Spans**: 4 sequential, non-overlapping `execute_tool` child spans:
  1. `fetch_kyc_status` (0.85s)
  2. `fetch_credit_score` (0.80s)
  3. `fetch_transaction_history` (0.90s)
  4. `fetch_fraud_blacklist` (0.85s)
- **Cumulative Duration**: ~3.4s - 4.9s

#### 2. Expected Auto-Fix
- **Action**: Wrap the independent fetch calls in `asyncio.gather()` to execute concurrently.

```diff
# BEFORE (Sequential waterfall I/O)
- kyc = await fetch_kyc_status(user_id)
- credit = await fetch_credit_score(user_id)
- history = await fetch_transaction_history(user_id)
- blacklist = await fetch_fraud_blacklist(user_id)

# AFTER (Parallelized I/O execution)
+ kyc, credit, history, blacklist = await asyncio.gather(
+     fetch_kyc_status(user_id),
+     fetch_credit_score(user_id),
+     fetch_transaction_history(user_id),
+     fetch_fraud_blacklist(user_id),
+ )
```

```
====================================================================================================
```

### Scenario 10: Uncached Repeated I/O Lookup
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L928-L1005)
- **Function**: `run_uncached_repeated_io_scenario`
- **Latency Type**: `UNCACHED_REPEATED_IO`

#### 1. Diagnostic Parameters
- **Spans**: 2 duplicate `fetch_live_fx_rates` tool calls with identical arguments `{"pair": "EUR_USD", "date": "2026-08-27"}` across `InvoiceCalculationAgent` and `TaxComplianceAgent`
- **Duplicate Delay**: 2 x 1.6s = 3.2s

#### 2. Expected Auto-Fix
- **Action**: Add an in-memory TTL cache or Redis decorator to the tool definition.

```diff
# BEFORE (No caching on idempotent external API)
- async def fetch_live_fx_rates(pair: str, date: str):
-     return await http_client.get(f"/rates?pair={pair}&date={date}")

# AFTER (In-memory TTL cache decorator)
+ from async_lru import alru_cache
+
+ @alru_cache(maxsize=128, ttl=3600)
+ async def fetch_live_fx_rates(pair: str, date: str):
+     return await http_client.get(f"/rates?pair={pair}&date={date}")
```

```
====================================================================================================
```

### Scenario 11: Heavy Vision Payload I/O
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L1008-L1080)
- **Function**: `run_heavy_vision_payload_scenario`
- **Latency Type**: `HEAVY_VISION_PAYLOAD_IO`

#### 1. Diagnostic Parameters
- **Span Name**: `agent_heavyvisionagent_process_high_res_vision_document`
- **Tool**: `base64_vision_payload_transfer`
- **Payload Size**: `25.4 MB` (`image/tiff`, 6000x4000 resolution)
- **Transfer Duration**: ~4.2s

#### 2. Expected Auto-Fix
- **Action**: Downscale image to max 1024px and encode as WebP before base64 transmission, reducing payload size from 25.4MB to < 350KB.

```diff
# BEFORE (Sending raw 25MB uncompressed TIFF image to vision LLM)
- base64_image = base64.b64encode(open("passport_raw.tiff", "rb").read()).decode()
- await vision_agent.process(base64_image)

# AFTER (Downscaling and compressing to WebP prior to transmission)
+ from PIL import Image
+ import io
+
+ img = Image.open("passport_raw.tiff")
+ img.thumbnail((1024, 1024))
+ buffer = io.BytesIO()
+ img.save(buffer, format="WEBP", quality=80)
+ compressed_b64 = base64.b64encode(buffer.getvalue()).decode()
+ await vision_agent.process(compressed_b64)
```

```
====================================================================================================
```

### Scenario 12: Clean Optimal Baseline (Control Reference)
- **File**: [`agent_handoff_scenarios.py`](file:///c:/Users/Himanshu/Downloads/Document-Ai-branch-vm2/backend-v2/trial-workflow/latencyTests/agent_handoff_scenarios.py#L646-L707)
- **Function**: `run_clean_baseline_scenario`
- **Latency Type**: `CLEAN_OPTIMAL_BASELINE`

#### 1. Diagnostic Parameters
- **Total Duration**: < 400ms - 850ms
- **Spans**: 2 clean 1:1 handoff spans (`ExpressIntakeAgent` $\rightarrow$ `ExpressDispatchAgent`)
- **Error Spans**: 0

#### 2. Expected Auto-Fix
- **Action**: **No Fix Required**. Verify that the Auto-Fix engine marks this trace as `CLEAN` / `OPTIMAL` with zero remediation proposals.
